# ============================================================================
# OpenClaw Multi-Model Routing - Environment Variables
# Business Partner Bot Configuration
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to ~/.openclaw/.env on your VPS
# 2. Fill in your actual API keys (remove the placeholder text)
# 3. Never commit the actual .env file to version control
#
# DEPLOYMENT PATH: ~/.openclaw/.env
# ============================================================================

# ============================================================================
# REQUIRED API KEYS
# ============================================================================

# ----------------------------------------------------------------------------
# Anthropic API Key (PRIMARY MODEL)
# ----------------------------------------------------------------------------
# Provider: Anthropic
# Used for: Claude Sonnet 4.5 (primary model)
# Cost: $3/M input, $15/M output
# 
# Where to get it:
# 1. Go to https://console.anthropic.com/
# 2. Sign up or log in
# 3. Navigate to "API Keys" section
# 4. Click "Create Key"
# 5. Copy the key (starts with "sk-ant-")
#
# Alternative: Use Claude Code CLI setup-token
# Run: claude setup-token
# Then: openclaw models auth paste-token --provider anthropic
#
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ----------------------------------------------------------------------------
# Google API Key (FALLBACK & IMAGE MODEL)
# ----------------------------------------------------------------------------
# Provider: Google AI Studio
# Used for: Gemini models (fallback, images, heartbeat)
# Cost: $0.075-$3.50/M depending on model
#
# Where to get it:
# 1. Go to https://makersuite.google.com/app/apikey
# 2. Sign in with Google account
# 3. Click "Create API Key"
# 4. Copy the key
#
GOOGLE_API_KEY=your-google-api-key-here

# ============================================================================
# OPTIONAL API KEYS (Recommended for Full Resilience)
# ============================================================================

# ----------------------------------------------------------------------------
# OpenAI API Key (FALLBACK MODEL)
# ----------------------------------------------------------------------------
# Provider: OpenAI
# Used for: GPT-4 Turbo (fallback model)
# Cost: $2.50/M input, $10/M output
#
# Where to get it:
# 1. Go to https://platform.openai.com/api-keys
# 2. Sign up or log in
# 3. Click "Create new secret key"
# 4. Copy the key (starts with "sk-")
#
# Note: Optional but recommended for provider diversity
#
OPENAI_API_KEY=sk-your-openai-key-here

# ----------------------------------------------------------------------------
# DeepSeek API Key (SUBAGENT MODEL)
# ----------------------------------------------------------------------------
# Provider: DeepSeek
# Used for: DeepSeek Reasoner (subagent tasks)
# Cost: $2.74/M (very cost-effective)
#
# Where to get it:
# 1. Go to https://platform.deepseek.com/
# 2. Sign up or log in
# 3. Navigate to API Keys section
# 4. Create new API key
# 5. Copy the key
#
# Note: Optional but recommended for cost-effective reasoning tasks
#
DEEPSEEK_API_KEY=your-deepseek-key-here

# ----------------------------------------------------------------------------
# OpenRouter API Key (FREE FALLBACK MODELS)
# ----------------------------------------------------------------------------
# Provider: OpenRouter
# Used for: Free fallback models (DeepSeek R1, Llama 3.3, Qwen Vision)
# Cost: $0 (free tier models)
#
# Where to get it:
# 1. Go to https://openrouter.ai/keys
# 2. Sign up or log in with GitHub
# 3. Click "Create Key"
# 4. Copy the key (starts with "sk-or-")
#
# Note: Optional but provides zero-cost emergency fallbacks
#
OPENROUTER_API_KEY=sk-or-your-key-here

# ============================================================================
# WEB TOOLS (Optional)
# ============================================================================

# ----------------------------------------------------------------------------
# Brave Search API Key (WEB SEARCH)
# ----------------------------------------------------------------------------
# Provider: Brave Search API
# Used for: web_search tool
# Cost: FREE (2,000 requests/month, credit card required for verification)
#
# Where to get it:
# 1. Go to https://brave.com/search/api/
# 2. Sign up for free tier
# 3. Add credit card for verification (no charges)
# 4. Copy API key
#
# Note: Optional - only needed if using web search functionality
#
BRAVE_API_KEY=your-brave-api-key-here

# ----------------------------------------------------------------------------
# Firecrawl API Key (WEB SCRAPING)
# ----------------------------------------------------------------------------
# Provider: Firecrawl
# Used for: Advanced web scraping (optional, falls back to direct fetch)
# Cost: Varies by plan
#
# Where to get it:
# 1. Go to https://firecrawl.dev/
# 2. Sign up for account
# 3. Get API key from dashboard
#
# Note: Optional - direct fetch is used if not configured
#
FIRECRAWL_API_KEY=your-firecrawl-key-here

# ============================================================================
# GATEWAY SECURITY
# ============================================================================

# ----------------------------------------------------------------------------
# OpenClaw Gateway Auth Token
# ----------------------------------------------------------------------------
# Used for: Securing OpenClaw API access
# 
# Generate a secure token:
# Option 1: openssl rand -hex 32
# Option 2: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Option 3: python3 -c "import secrets; print(secrets.token_hex(32))"
#
# This token is required for API access to your OpenClaw instance
#
OPENCLAW_AUTH_TOKEN=your-secure-random-token-here

# ============================================================================
# CHANNEL INTEGRATIONS (Optional)
# ============================================================================

# ----------------------------------------------------------------------------
# Twilio (WhatsApp Integration)
# ----------------------------------------------------------------------------
# Provider: Twilio
# Used for: WhatsApp channel integration
#
# Where to get credentials:
# 1. Go to https://console.twilio.com/
# 2. Sign up or log in
# 3. Get Account SID and Auth Token from dashboard
# 4. Set up WhatsApp sandbox or get approved WhatsApp number
#
# Note: Only needed if using WhatsApp channel
#
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_WHATSAPP_FROM=whatsapp:+1234567890

# ----------------------------------------------------------------------------
# Telegram Bot Token
# ----------------------------------------------------------------------------
# Provider: Telegram
# Used for: Telegram channel integration
#
# Where to get it:
# 1. Message @BotFather on Telegram
# 2. Send /newbot command
# 3. Follow instructions to create bot
# 4. Copy the bot token
#
# Note: Only needed if using Telegram channel
#
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# ----------------------------------------------------------------------------
# Discord Bot Token
# ----------------------------------------------------------------------------
# Provider: Discord
# Used for: Discord channel integration
#
# Where to get it:
# 1. Go to https://discord.com/developers/applications
# 2. Create new application
# 3. Go to "Bot" section
# 4. Click "Add Bot"
# 5. Copy the bot token
#
# Note: Only needed if using Discord channel
#
DISCORD_BOT_TOKEN=your-discord-bot-token-here

# ============================================================================
# SPEECH & VOICE (Optional)
# ============================================================================

# ----------------------------------------------------------------------------
# ElevenLabs API Key (Text-to-Speech)
# ----------------------------------------------------------------------------
# Provider: ElevenLabs
# Used for: High-quality text-to-speech
#
# Where to get it:
# 1. Go to https://elevenlabs.io/
# 2. Sign up or log in
# 3. Navigate to API settings
# 4. Copy your API key
#
# Note: Only needed if using voice/TTS features
#
ELEVENLABS_API_KEY=your-elevenlabs-key-here

# ============================================================================
# GOG CLI (Google Workspace Access)
# ============================================================================
#
# The gog CLI (https://github.com/stacklok/gog) provides Gmail, Calendar,
# Drive, Docs, and Sheets access via the exec tool.
#
# IMPORTANT: This password must match the one used when running:
#   gog auth add --manual --force-consent your-email@domain.com
#
# The password encrypts the OAuth refresh token stored by gog.
# Set to empty string if you want no password (less secure).
#
GOG_KEYRING_PASSWORD=your-keyring-password-here
GOG_ACCOUNT=your-email@domain.com

# ============================================================================
# NOTES
# ============================================================================
#
# REQUIRED vs OPTIONAL:
# - REQUIRED: ANTHROPIC_API_KEY, GOOGLE_API_KEY
# - RECOMMENDED: OPENAI_API_KEY, DEEPSEEK_API_KEY, OPENROUTER_API_KEY
# - OPTIONAL: All others (based on features you use)
#
# SECURITY:
# - Never commit this file with real keys to version control
# - Use restrictive file permissions: chmod 600 ~/.openclaw/.env
# - Rotate keys periodically
# - Use separate keys for development and production
#
# COST MONITORING:
# - Set up billing alerts in each provider's dashboard
# - Monitor usage with: openclaw status --usage
# - Review monthly costs in provider dashboards
#
# TESTING:
# - Test each provider separately before full deployment
# - Use openclaw models status to verify auth
# - Start with small requests to verify configuration
#
# ============================================================================
